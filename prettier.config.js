module.exports = {
  // 单行长度
  printWidth: 120,
  // 缩进长度
  tabWidth: 2,
  // 使用空格代替tab缩进
  useTabs: false,
  // 句末使用分号
  semi: false,
  // 使用单引号
  singleQuote: true,
  // 仅在必需时为对象的key添加引号
  quoteProps: 'as-needed',
  // jsx中使用单引号
  jsxSingleQuote: true,
  // 在对象前后添加空格
  bracketSpacing: true,
  // 在 ES5 中有效的尾后逗号（如对象与数组等）
  trailingComma: 'es5',
}
